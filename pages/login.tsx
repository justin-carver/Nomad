import { Paper, Center, Container } from '@mantine/core';
import styles from '../styles/login.module.css';

const Login = () => {
	return (
		<Center className={styles['Login']}>
			<Container className={styles['Login__container']}>Testing</Container>
		</Center>
	);
};

export default Login;
